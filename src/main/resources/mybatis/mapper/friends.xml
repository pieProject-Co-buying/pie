<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.pie.pieProject.DAO.IFriendDao">

	<!-- 팔로잉 -->
	<insert id="Following">
		INSERT INTO pie_friendTable VALUES (#{param1},#{param2})
	</insert>
	<!-- 언팔로우 -->
	<delete id="unFollow">
		delete pie_friendTable where followingId = #{param1} and
		followedId = #{param2}
	</delete>
	<!-- 팔로잉 여부 체크하기 -->
	<select id="checkFollow" resultType="_int">
		select count(*) from
		pie_friendTable where followingId = #{param1} and followedId = #{param2}
	</select>
	<!-- 내 팔로잉 리스트 체크하기 -->
	<select id="friendsList"
		resultType="com.pie.pieProject.DTO.MemberDto">
		SELECT pie_member_user.nickname, pie_member_user.id,
		pie_member_user.profile_pic
		FROM pie_friendTable
		JOIN pie_member_user ON
		pie_friendTable.followedId = pie_member_user.id
		WHERE pie_friendTable.followingid =
		#{param1}
	</select>
	<delete id="outMemberFollow">
		delete pie_friendTable where followingId=#{param1} or followedId = #{param2}
	</delete>

</mapper>