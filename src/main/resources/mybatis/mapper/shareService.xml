<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pie.pieProject.DAO.IShareServiceDao">
	<!--게시판 리스트 최신순 불러오기 -->
	<select id="getBoardList"
		resultType="com.pie.pieProject.DTO.ShareServiceDto">
		select * from shareServiceBoard order by sh_num desc     
	</select>
	<!--해당 게시물 상세 페이지 -->
	<select id="selectBoard"
		resultType="com.pie.pieProject.DTO.ShareServiceDto">
		select * from shareServiceBoard where sh_num=#{param1}
	</select>
	<!--게시물 제목,내용 검색-->
	<select id="searchBoard" resultType="com.pie.pieProject.DTO.ShareServiceDto">
		select * from shareServiceBoard where sh_title like '%${param1}%' or sh_content like '%${param1}%'
	</select>
	<!--상세 검색 -->
	<select id="searchTitle" resultType="com.pie.pieProject.DTO.ShareServiceDto">
		select * from shareServiceBoard where sh_category = '${param1}' and (sh_title like '%${param2}%' OR sh_content like '%${param2}%')
	</select>
	<!--카테고리 검색 -->
	<select id="searchCategory" resultType="com.pie.pieProject.DTO.ShareServiceDto">
		select * from shareServiceBoard where sh_category = '${param1}'
	</select>
	<!--내가 쓴 게시글 -->
	<select id="myBoard"
		resultType="com.pie.pieProject.DTO.ShareServiceDto">
		select * from shareServiceBoard where sh_id=#{param1} order
		by sh_registDay desc
	</select>
	<!--결제 완료 페이지 -->
	<select id="completePay"
		resultType="com.pie.pieProject.DTO.ShareServiceDto">
		select * from shareServiceBoard where sh_num=#{sh_numID}
	</select>
	<!--게시글 작성 -->
	<insert id="insertBoard">
		<!--insert into shareServiceBoard (sh_numID,sh_id,sh_nickname,sh_title,sh_content,sh_category,sh_price,sh_process,sh_DeadLine,sh_personnelNow,sh_personnelMax,sh_filename,sh_product) 
			values (sh_numID.nextval,#{sh_id},#{sh_nickname},#{sh_title},#{sh_content},#{sh_category},#{sh_price},#{sh_process},TO_DATE(#{sh_DeadLine}, 
			'YYYY-MM-DD'),#{sh_personnelNow},#{sh_personnelMax},#{sh_filename},sh_product.nextval) -->
		insert into shareServiceBoard values(
		sh_num.nextval, #{sh_id}, #{sh_nickname}, #{sh_category}, #{sh_premium},
		#{sh_title}, #{sh_content}, #{sh_profileImg}, #{sh_productImg}, 0,
		#{sh_tag}, '1', sysdate, sysdate, TO_DATE(#{sh_deadLine}, 'YYYY-MM-DD'),
		#{sh_personnelMax}, 1, #{sh_priceTotal}, #{sh_pricePer}, #{sh_ip}, 0,
		'001' )
	</insert>
	<!--게시글 수정 -->
	<update id="updateBoard">
		update shareServiceBoard set
		sh_category=#{sh_category},
		sh_title=#{sh_title},
		sh_content=#{sh_content},
		sh_productImg=#{sh_productImg},
		sh_tag=#{sh_tag},
		sh_deadLine=TO_DATE(#{sh_deadLine}, 'YYYY-MM-DD'),
		sh_personnelMax=#{sh_personnelMax},
		sh_priceTotal=#{sh_priceTotal},
		sh_pricePer=#{sh_pricePer},
		sh_ip=#{sh_ip},
		sh_updateDay=sysdate where
		sh_num=#{sh_num}
	</update>
	<!-- 현재 인원 증가 -->
	<update id="updateNow">
		update shareServiceBoard set sh_personnelNow =
		sh_personnelNow + 1 where sh_num = #{param1}
	</update>
	<!--게시글 삭제 -->
	<delete id="deleteBoard" parameterType="_int">
		delete shareServiceBoard
		where sh_num=#{param1}
	</delete>
	<!--최대인원 체크 -->
	<update id="maxChk">
		update shareServiceBoard set sh_process = '0' where
		sh_num = #{param1}
	</update>
	<!--최소인원 체크 -->
	<update id="minChk">
		update shareServiceBoard set sh_process = '1' where
		sh_num = #{param1}
	</update>
	<!-- 모집중단 -->
	<update id="stopChk">
		update shareServiceBoard set sh_process = '2' where
		sh_num = #{param1}
	</update>
	<!--기간만료-->
 	<update id="dateOver">
		update shareServiceBoard set sh_process = '3' where 
		sh_num = #{param1}
	</update>
	<!--조회수 증가 -->
	<update id="updateHit">
		update shareServiceBoard set sh_hit = sh_hit+1 where
		sh_num = #{param1}
	</update>
	<!--작성자 이름 및 아이디로 검색 -->
	<select id="searchBuyer">
		select * from shareServiceBoard where sh_id like
		'%${param1}%' or sh_nickname like '%${param1}%'
	</select>
	<!--환불시 인원 감소 -->
	<update id="refundNowPerson">
		update shareServiceBoard set sh_personnelNow =
		sh_personnelNow - 1 where sh_num = #{param1}
	</update>
	<!-- 0시마다 마감체크 -->
	<update id="expireShareServiceBoard">
		UPDATE shareServiceBoard
		SET sh_process = 0
		WHERE sh_deadLine <![CDATA[ <= ]]>
		sysdate
	</update>
</mapper>