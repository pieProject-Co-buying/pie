<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pie.pieProject.DAO.IPaymentDAO">
	<!--결제 데이터 베이스-->
	<insert id="insertPayment" parameterType="com.pie.pieProject.DTO.PaymentDTO">
        insert into payment (buyer_name,buyer_nickname,buyer_tel,buyer_addr,buyer_email,buyer_postcode,pay_uid, pay_method, pay_merchant_uid, pay_name, pay_amount, pay_date) values (#{buyer_name},#{buyer_nickname},#{buyer_tel},#{buyer_addr},#{buyer_email},#{buyer_postcode},#{pay_uid}, #{pay_method}, #{payMerchant_uid}, #{payName}, #{payAmount}, sysdate)
    </insert>
    <select id="findPay">
    	select * from payment where pay_method=#{pay_method}
    </select>
</mapper>



