<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.pie.pieProject.DAO.IFriendDao">


	<!-- 메세지 저장 -->
	<insert id="Following">
		INSERT INTO friendTable VALUES (#{param1},#{param2})
	</insert>
	<delete id="unFollow">
		delete friendTable where followingId = #{param1} and
		followedId = #{param2}
	</delete>

	<select id="checkFollow" resultType="_int">
		select count(*) from
		friendTable where followingId = #{param1} and followedId = #{param2}
	</select>

	<select id="friendsList" resultType="com.pie.pieProject.DTO.MemberDto">
		SELECT member_user.nickname, member_user.id, member_user.profile_pic
		FROM friendTable
		JOIN member_user ON friendTable.followedId = member_user.id
		WHERE friendTable.followingid = #{param1}
	</select>

</mapper>